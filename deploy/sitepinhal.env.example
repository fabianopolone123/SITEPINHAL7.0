# Exemplo de variaveis de ambiente (NAO comitar segredos reais aqui).
#
# Copie este arquivo para o servidor como /etc/sitepinhal.env e ajuste.
#
DJANGO_SECRET_KEY=troque-por-uma-chave-forte
DJANGO_DEBUG=0

# Coloque seu dominio e/ou IP (separados por virgula).
DJANGO_ALLOWED_HOSTS=145.223.93.162,seu-dominio.com

# Quando usar HTTPS (recomendado), inclua o(s) origem(ns) confiavel(is).
DJANGO_CSRF_TRUSTED_ORIGINS=https://seu-dominio.com

# Para manter dados fora da pasta do codigo (deploy mais seguro):
DJANGO_MEDIA_ROOT=/srv/sitepinhal/media
DJANGO_STATIC_ROOT=/srv/sitepinhal/static
DJANGO_SQLITE_PATH=/srv/sitepinhal/data/db.sqlite3

# Opcional (se decidir usar Postgres no futuro):
# DJANGO_DATABASE_URL=postgresql://usuario:senha@127.0.0.1:5432/sitepinhal

# Se quiser forcar HTTPS pelo Django (alem do redirecionamento do nginx):
DJANGO_SECURE_SSL_REDIRECT=1
